package lxd.command.demo;

/**
 * javap是jdk自带的一个工具，可以对代码反编译，也可以查看java编译器生成的字节码。
 * 
 * @author lxd
 * @date 2017年2月3日
 */
public class JavapDemo {
	
	/*
	 * javap命令分解一个class文件，它根据options来决定到底输出什么。
	 * 如果没有使用options,那么javap将会输出包，类里的protected和public域以及类里的所有方法。
	 * javap将会把它们输出在标准输出上。
	 */
	
	public static void main(String[] args) {
		/*
		 * javap -c *.class 将class文件反汇编  （常用）
		 */
	}
	
}
